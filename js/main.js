import { initUI } from './ui.js';

document.addEventListener('DOMContentLoaded', () => {
    // Initialize the application UI
    initUI();
    
    
    console.log("SMA PGRI JASINGA BOGOR CUP 2026 Tournament Generator Initialized");
});
